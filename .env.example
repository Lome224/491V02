# Serial port for the Arduino Nano motor controller.
# Copy to .env (uncommitted) and fill in the path shown by `just ports`.

BOARD_PORT=/dev/cu.usbserial-2140
BOARD_BAUD=115200
SENSOR_PORT=/dev/cu.usbserial-21110
SENSOR_BAUD=115200
