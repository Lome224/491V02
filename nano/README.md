# Arduino Nano Motor Control

Standalone PlatformIO project for the Arduino Nano version of the motor
controller firmware.

- Folder: `nano/`
- Board selector: `nano`
- PlatformIO env: `nano`
- Main sketch: `src/main.cpp`

From the repo root:

```bash
just build nano
just upload nano
just monitor nano
just probe nano
```
